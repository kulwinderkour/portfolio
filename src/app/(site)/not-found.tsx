import { NotFoundContent } from "@/components/NotFoundContent";

/** Raised by notFound() inside the site — the group layout supplies the chrome. */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
